# Test scenarios

- Marks exactly one public route as current.
- Shows the correct operational label.
- Exposes an accessible cart button only when a callback is supplied.
- Keeps all four destinations visible in a compact mobile row.
