# Direct dependencies

- `cloudflare:workers`: access to the D1 binding in server code.
- React / Next App Router: server rendering and UI composition.
- `modules/catalog`: future source of catalog administration rules.
- `modules/ordering`: future cart and checkout consumer.
