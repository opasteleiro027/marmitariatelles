# Dependências

- Snapshot público do cardápio.
- Endpoint `POST /api/orders`.
- Regra de formatação monetária.
- Campos de endereço do módulo `address-location`.
- `menu-builder`, que pode fornecer a observação opcional do cliente.
