# Dependências

- `modules/admin-auth` para encerrar a sessão;
- `modules/brand/ui/brand-logo` para a identidade oficial;
- `admin-navigation` para destinos e estado ativo;
- layout do grupo `app/admin/(panel)`.
