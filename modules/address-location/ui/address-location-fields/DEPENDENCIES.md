# Dependências

- contrato público de áreas do storefront;
- endpoints internos `/api/address/postal-code/:postalCode` e
  `/api/address/reverse`;
- Geolocation API do navegador;
- regras de normalização e correspondência do módulo `address-location`.
